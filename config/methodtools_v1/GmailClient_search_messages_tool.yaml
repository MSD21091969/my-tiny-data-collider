name: search_messages_tool
description: Tool wrapper for GmailClient.search_messages with validation and execution
  control
category: communication_management
version: 1.0.0
tags:
- communication
- search
- search
method_reference:
  service: GmailClient
  method: search_messages
  classification:
    domain: communication
    subdomain: gmail
    capability: search
    complexity: atomic
    maturity: beta
    integration_tier: external
method_params:
- name: request
  type: object
  required: true
  description: Request object
tool_params:
- name: timeout_seconds
  type: integer
  required: false
  description: Maximum execution time in seconds
  default: 30
  min_value: 5
  max_value: 300
- name: dry_run
  type: boolean
  required: false
  description: Preview mode without actual execution
  default: false
implementation:
  type: method_wrapper
  method_wrapper:
    method_name: GmailClient.search_messages
    parameter_mapping:
      method_params:
      - request
      tool_params:
      - timeout_seconds
      - dry_run
business_rules:
  enabled: true
  requires_auth: true
  required_permissions:
  - workspace:gmail:read
  requires_casefile: false
  timeout_seconds: 30
examples:
- description: Basic search_messages operation
  input:
    request: sample_request
  expected_output:
    result: success
    data: {}
data_contracts:
  request_model: GmailSearchMessagesRequest
  response_model: GmailSearchMessagesResponse
  canonical_models: []
  module: src.pydantic_ai_integration.integrations.google_workspace.models
dependencies:
  methods:
  - search_messages
  models:
  - GmailSearchMessagesRequest
  - GmailSearchMessagesResponse
  services:
  - GmailClient
compatibility:
  method_version: 1.0.0
  schema_version: '1.0'
  requires_auth: true
